# tuples
# they are same as list
# the only difference is that:
# list are mutable objects, i.e the values/elements in the list can be modified
# tuples are immutable objects, i.e the values/elements in the tuple cannot be modified

# making a tuples
dimensions = (1920, 1080)

# the difference b/w tuple & list was asked in my Infosys Interview
# they also asked for the one live example where we will use tuple and where we will be using the list
